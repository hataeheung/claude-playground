# CLAUDE.md - 에이전틱 코딩 프로세스

이 프로젝트에서 모든 코딩 작업은 반드시 아래 6단계 프로세스를 따른다.
사용자가 어떤 기능 요청이나 버그 수정을 요청하면, 바로 코드를 작성하지 말고 1단계부터 순서대로 진행한다.

---

## 프로세스: 6단계

### Phase 1: ANALYZE (분석)
> 핵심 질문: "무엇을 만들어야 하는가?"

작업을 시작하기 전에 반드시:
- 사용자의 요구사항을 자신의 말로 정리하여 확인받기
- 모호한 부분이 있으면 구현 전에 질문하기
- 기능적/비기능적 요구사항 구분하기
- 제약 조건 파악 (기술 스택, 호환성 등)
- 완료 기준(Done criteria)을 명시하기

이 단계를 **사용자에게 보여주고** 확인을 받은 후 다음 단계로 진행한다.

### Phase 2: EXPLORE (탐색)
> 핵심 질문: "현재 상태는 어떠한가?"

코드를 수정하기 전에 반드시:
- 프로젝트 구조 파악 (Glob, Read 도구 활용)
- 관련 파일을 모두 읽기 (수정할 파일 + 연관 파일)
- 기존 코드 패턴과 컨벤션 이해하기
- 의존성(dependencies) 확인하기
- 기존 테스트가 있으면 확인하기
- 변경의 영향 범위(blast radius) 파악하기

### Phase 3: PLAN (계획)
> 핵심 질문: "어떻게 만들 것인가?"

구현 전에 반드시:
- TodoWrite 도구로 작업을 분해하여 TODO 리스트 생성
- 구현 접근 방식을 사용자에게 설명하기
- 대안이 있다면 장단점과 함께 제시하기
- 작업 순서와 의존성을 정하기
- 위험 요소가 있으면 미리 알리기

### Phase 4: IMPLEMENT (구현)
> 핵심 질문: "코드를 작성한다"

구현 시 반드시:
- TODO 리스트에 따라 한 번에 하나의 작업만 진행
- 각 작업 완료 시 TodoWrite로 상태 업데이트
- 기존 코드 스타일과 패턴을 따르기
- 불필요한 복잡성 추가하지 않기 (YAGNI)
- 보안 취약점 주의 (injection, XSS 등)
- 각 변경 후 동작 확인

### Phase 5: VERIFY (검증)
> 핵심 질문: "제대로 동작하는가?"

코드 작성 후 반드시:
- 기존 테스트 실행하여 회귀(regression) 확인
- 새로운 기능에 대한 테스트 작성 (해당 시)
- 린터/포매터 실행
- 엣지 케이스 검토
- 원래 요구사항과 비교하여 누락 확인

### Phase 6: DELIVER (전달)
> 핵심 질문: "변경사항을 전달한다"

완료 시:
- 변경사항을 사용자에게 요약하여 보고
- 무엇이 바뀌었고, 왜 바뀌었는지 설명
- 추가 고려사항이나 후속 작업이 있으면 안내

---

## 단계 표시 규칙

각 단계를 진행할 때 아래 형식으로 현재 단계를 표시한다:

```
[ANALYZE] 요구사항을 정리하겠습니다...
[EXPLORE] 관련 코드를 탐색하겠습니다...
[PLAN] 구현 계획을 세우겠습니다...
[IMPLEMENT] 구현을 시작하겠습니다...
[VERIFY] 검증하겠습니다...
[DELIVER] 변경사항을 정리하겠습니다...
```

## 프로세스 흐름

```
ANALYZE → EXPLORE → PLAN → IMPLEMENT → VERIFY → DELIVER
   ↑                          ↓
   └──────── feedback ────────┘
```

구현 중 요구사항이 불명확하면 ANALYZE로 돌아가고,
예상치 못한 코드를 발견하면 EXPLORE로 돌아간다.

## 원칙

- 코드를 수정하기 전에 반드시 먼저 읽는다
- 한 번에 하나의 작업만 진행한다
- 기존 패턴과 컨벤션을 존중한다
- 최소한의 변경으로 목표를 달성한다 (YAGNI)
- 각 변경 후 반드시 검증한다
- 과도한 엔지니어링을 하지 않는다
